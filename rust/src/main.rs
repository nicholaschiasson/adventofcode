#![allow(clippy::let_unit_value, unreachable_code, unused_variables)]

use adventofcode::utils::{read_resource, relative_input_path};

fn main() {
    let year = todo!();
    let day = todo!();
    let input_path = relative_input_path("adventofcode::year_{year}::day_{day}::tests::final");
    let input = read_resource(input_path);
    println!("{input}");
}
